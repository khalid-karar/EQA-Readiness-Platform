# Pre-pilot UX polish

Items to address before pilot or real-data onboarding. Not blocking feature steps; tracked here so they are not lost.

- [ ] **Deep-link / shareable URLs** — link directly to a specific item or standard on the dashboard and remediation screens (e.g. `?item=` or `?standard=` query param), so selection state survives refresh and can be shared in review meetings.
