/**
 * @eqa/ai
 *
 * A swappable AI adapter interface. No vendor is hardcoded, and client data
 * must not leave Saudi Arabia. AI output is draft work product only; every call
 * records a redacted input summary, output, prompt/rubric versions, adapter,
 * timestamp, and the human reviewer decision trail.
 *
 * Skeleton only — no implementation yet (Step 1).
 */

export const PACKAGE_NAME = "@eqa/ai";
