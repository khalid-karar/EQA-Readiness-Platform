/**
 * @eqa/jobs
 *
 * Tenant-scoped background job framework. All long-running work (AI review,
 * evidence/document processing, exports, file scanning) runs here, never inside
 * synchronous request handlers.
 *
 * Skeleton only — no implementation yet (Step 1).
 */

export const PACKAGE_NAME = "@eqa/jobs";
