/**
 * @eqa/storage
 *
 * File storage with a hard safety gate: uploads are quarantined until they pass
 * file-type validation, size limits, and malware scanning. Nothing is made
 * available before it is cleared.
 *
 * Skeleton only — no implementation yet (Step 1).
 */

export const PACKAGE_NAME = "@eqa/storage";
